apiVersion: v1
kind: Service
metadata:
  name: {{ template "javasp.name" . }}
  labels:
{{- include "javasp.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5002
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "javasp.name" . }}
